export type Storage = {
    id: string,
    name: string,
    storageTypeId: string,
    typeKey: string,
    typeLabel: string,
    isDefault: boolean,
    isPublic: boolean,
    createdAt: string,
    updatedAt: string,
}